# Next 14 / wagmi / vitest 5 haben inkompatible Peer-Ranges (z.B. vitest -> @types/node ^22 || >=24).
# CI nutzt bereits --legacy-peer-deps; hier projektweit, damit jeder Build (Vercel etc.) gleich installiert.
legacy-peer-deps=true
